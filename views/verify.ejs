<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Membership Verification - RMAS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .verification-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
            margin: 2rem auto;
            max-width: 600px;
        }
        .header {
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            color: white;
            padding: 2rem;
            text-align: center;
        }
        .logo {
            max-width: 100px;
            margin-bottom: 1rem;
        }
        .member-details {
            padding: 2rem;
        }
        .member-photo {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #e9ecef;
            margin: 0 auto 1rem;
            display: block;
        }
        .status-badge {
            display: inline-block;
            padding: 0.5rem 1rem;
            border-radius: 25px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .status-valid {
            background: #d4edda;
            color: #155724;
        }
        .status-invalid {
            background: #f8d7da;
            color: #721c24;
        }
        .qr-code {
            text-align: center;
            margin: 2rem 0;
        }
        .qr-code img {
            max-width: 150px;
            border: 2px solid #dee2e6;
            border-radius: 8px;
            padding: 10px;
        }
        .member-info {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 1.5rem;
            margin: 1rem 0;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 1px solid #dee2e6;
        }
        .info-row:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        .info-label {
            font-weight: bold;
            color: #495057;
        }
        .info-value {
            color: #212529;
        }
        .footer {
            background: #f8f9fa;
            padding: 1rem;
            text-align: center;
            color: #6c757d;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="verification-card">
            <div class="header">
                <img src="/images/logo.jpeg" alt="RMAS Logo" class="logo">
                <h1>National Human Rights Association</h1>
                <p>Bihar State Branch - Membership Verification</p>
            </div>

            <% if (valid) { %>
                <div class="member-details">
                    <div class="text-center mb-4">
                        <span class="status-badge status-valid">✓ Valid Membership</span>
                    </div>

                    <% if (membership.photo) { %>
                        <img src="<%= membership.photo %>" alt="Member Photo" class="member-photo">
                    <% } %>

                    <div class="member-info">
                        <div class="info-row">
                            <span class="info-label">Full Name:</span>
                            <span class="info-value"><%= membership.fullName %></span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Membership ID:</span>
                            <span class="info-value"><%= membership.membershipId %></span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">District:</span>
                            <span class="info-value"><%= membership.district %></span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Mobile:</span>
                            <span class="info-value"><%= membership.mobile %></span>
                        </div>
                        <% if (membership.email) { %>
                            <div class="info-row">
                                <span class="info-label">Email:</span>
                                <span class="info-value"><%= membership.email %></span>
                            </div>
                        <% } %>
                        <div class="info-row">
                            <span class="info-label">Issue Date:</span>
                            <span class="info-value"><%= new Date(membership.createdAt).toLocaleDateString() %></span>
                        </div>
                    </div>

                    <div class="qr-code">
                        <img src="<%= qrCode %>" alt="QR Code">
                        <p class="mt-2 text-muted">Scan to verify this membership</p>
                    </div>

                    <div class="text-center">
                        <a href="<%= membership.pdfUrl %>" class="btn btn-primary btn-lg" target="_blank">
                            📄 Download Membership Certificate
                        </a>
                    </div>
                </div>
            <% } else { %>
                <div class="member-details text-center">
                    <div class="mb-4">
                        <span class="status-badge status-invalid">✗ Invalid or Pending ID</span>
                    </div>
                    <div class="alert alert-warning">
                        <h4>The membership ID you entered is not valid or the application is still pending approval.</h4>
                        <p>Please check your membership ID and try again, or contact RMAS administration if you believe this is an error.</p>
                    </div>
                    <a href="/" class="btn btn-secondary">Return to Home</a>
                </div>
            <% } %>

            <div class="footer">
                <p>&copy; 2025 National Human Rights Association, Bihar. All rights reserved.</p>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
